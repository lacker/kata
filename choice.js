// Pick k items from the list randomly
function choice(list, k) {
  if (list.length > k) {
    throw new Error(
      'cannot choose ' + k +
      ' from a list of length ' + list.length);
  }
  // TODO
}
